# New session - 2026-06-17T00:02:27.789Z (build)

Tokens: 218172 in / 7404 out

## User

Review recent repository changes and update documentation so it matches the implementation.

Start by inspecting recent history:

    git log --since="24 hours ago" --no-merges --pretty=format:"%h %ad %s" --date=iso

Read the project overview and documentation sources that exist in this repository, such as `README.md`, `docs/`, `AGENTS.md`, `CONTRIBUTING.md`, or equivalent files.

Update docs that are outdated, incomplete, or inaccurate. Keep the existing structure, tone, and formatting. Make the smallest accurate changes. Do not describe planned work as shipped behavior.

If documentation changes are made, create a branch named `docs/nightly-update-YYYY-MM-DD`, commit with message `docs: update documentation from recent changes`, push, and open a pull request against `main`.

Add this footer to the pull request description, resolving the environment variables $CHETTER_AGENT_NAME, $CHETTER_MODEL_ID, $CHETTER_RUNNER_IMAGE, and $CHETTER_RUNNER_IMAGE_DIGEST:

    ---
    Generated by [Chetter](https://github.com/flatout-works/chetter)
    Agent: $CHETTER_AGENT_NAME | Model: $CHETTER_MODEL_ID | Runner: $CHETTER_RUNNER_IMAGE ($CHETTER_RUNNER_IMAGE_DIGEST)

If no documentation changes are needed, report that and exit without a PR. Do not push directly to `main`.

## Assistant

<details><summary>Reasoning</summary>

Let me start by inspecting recent git history and understanding the repository structure.

</details>

*Tokens: 11977 total, 11735 in, 228 out*

## Assistant

<details><summary>Reasoning</summary>

Let me get a clear picture of what changed in the last 24 hours and assess which documentation files might need updating. Let me inspect the specific changes more closely.

</details>

*Tokens: 15024 total, 2869 in, 218 out*

## Assistant

<details><summary>Reasoning</summary>

Let me now read the documentation files that were changed in the last 24 hours along with the unchanged ones to understand what's outdated. The files modified in the last 24 hours include:

- `AGENTS.md` - repo-specific guidance
- `CHANGELOG.md` - changelog
- `README.md` - readme
- `docs/DAYTONA.md` - might have been updated
- `docs/FEATURES.md` - might have been updated
- `docs/MANUAL.md` - might have been updated
- `docs/REVIEWS.md` - might have been updated

Let me read these doc files and the git diff to understand what implementation changes need doc updates.

</details>

*Tokens: 16575 total, 15733 in, 693 out*

## Assistant

<details><summary>Reasoning</summary>

Let me analyze the recent changes in detail and check which documentation files may be outdated.

From the git log in the last 24 hours, key changes include:

1. **gVisor support + Kubernetes manifests** - `feat(runner): add gVisor execution support`, `feat(deploy): add Kubernetes manifests with gVisor support`, `feat(deploy): add Docker socket mount and gVisor flag to compose`, `docs: add k3s local testing guide and gVisor sandbox docs`

2. **Remove Kata/containerd execution mode** - `refactor(runner): remove Kata/containerd execution mode`

3. **Runner image variants and CI change detection** - `feat: add runner image variants and CI change detection`

4. **Claude Code harness** - `feat(runner): add Claude Code harness as alternative to OpenCode`

5. **Session export via SQLite** - `feat(runner): read session export from opencode SQLite db`

6. **Runner SSE fix** - `fix(runner): use bufio.Reader instead of Scanner for SSE events`

7. **CACHEBUST build arg fix** - `fix(build): add CACHEBUST arg to force full rebuild`

Let me check what's really important:

Key changes with doc implications:
1. **Kata removed** - The commit `f6a70d2 refactor(runner): remove Kata/containerd execution mode` means Kata is no longer a thing. 
2. **gVisor added** - gVisor is now the primary sandbox runtime.
3. **Claude Code harness** - documented in FEATURES.md but may need review.
4. **Session export** - documented in FEATURES.md.

Let me look at the FEATURES.md file more closely. The "Execution Environments" section still mentions Kata as the default:

```
## Execution Environments

### Kata Containers (default)

Tasks run in Kata Containers via containerd with full network isolation:
```

This is clearly wrong now - Kata was removed. gVisor is now the sandbox path and Docker is the default execution mode.

Let me also check the README.md sandbox section - it still references gVisor through containerd:
"The runner integrates gVisor through containerd — the same `ctr` interface already used for other runtimes..."

And the comparison table still mentions Kata.

Let me be very specific about what has changed. Let me check the actual code changes.

</details>

Let me look at the actual code changes to understand the current state precisely.

*Tokens: 58143 total, 40788 in, 321 out*

## Assistant

<details><summary>Reasoning</summary>

Now I have a clear picture of what changed. Let me compile the issues:

### 1. `docs/FEATURES.md` — Multiple issues

**A. Execution Environments section (lines 136-154)**

Still mentions Kata Containers as the default. Kata has been removed entirely. The section now should mention:
- Docker (default) — tasks run in Docker containers
- gVisor (optional) — kernel-level isolation via `--runtime=runsc`
- Local — tasks run directly on host

**B. "Kata Containers (default)" title showing on line 137**

This is wrong.

**C. The MCP tool count — currently says "26 tools" (line 288)**

Need to check if the count is still accurate.

**D. Runner Configuration table (lines 382-397)**

Still references `runner.yaml` fields for `execution.harness` and old fields. But specifically the `execution.harness` is correct, it's still there. The table mentions `runtime` which was for Kata/Docker/Local selection - that may have changed.

Let me check: the Kata removal removed `runKataAgent()` but kept `runDockerAgent()` and `runLocalAgent()`. The `Runtime` field in config still exists but Kata is gone. Let me check what's left.

Let me look at the current runner config to see what `Runtime` field values are valid after the change.

### 2. `README.md` — Several outdated references

**A. Sandbox Isolation section (lines 613-739)**

The gVisor integration description on line 625 says "The runner integrates gVisor through containerd..." — but gVisor now integrates through Docker (`--runtime=runsc`), not through containerd. This is outdated.

**B. The comparison table (lines 629-635)** still has a row for "Kata Containers" which has been removed. Should be updated to reflect current state.

**C. The section about "Kata Containers were removed from Chetter" (line 637-638)** is fine since it says they were removed — that's accurate.

**D. The "Enabling gVisor on a standard Kubernetes cluster" section (lines 645-731)** describes gVisor via containerd DaemonSet, but gVisor now ships via Docker's runsc, not containerd shim. Actually wait, let me re-read the code changes more carefully...

The commit `b5eb27f` says "passes `--runtime=runsc` to `docker run`" — so gVisor is used via the Docker runtime, not containerd. The gVisor section in README.md still describes the containerd-based approach. However, the deploy/k8s directory has gVisor RuntimeClass manifests which are Kubernetes-level, so both approaches might coexist.

Let me check if the gVisor approach for Docker is described correctly. In the README:

Line 625: "The runner integrates gVisor through containerd — the same `ctr` interface already used for other runtimes, just with `--runtime io.containerd.runsc.v1` instead of `--runtime io.containerd.runc.v2`."

But after Kata removal and gVisor via Docker changes, the runner doesn't use containerd anymore. The gVisor integration is now through Docker's `--runtime=runsc` flag. So this is wrong.

**E. K3s section (lines 158-313)** references Kata in the comparison table end indirectly. Actually the k3s section seems fine on its surface.

### 3. `docs/DAYTONA.md` — Fine, it's a proposal

But the proposal references Kata as a default backend: "Eliminates infra management — Removes ~1500 lines of runner code (containerd/, network/, bridge management). No more ctr, Kata, iptables, veth pairs."

Since Kata has been removed already, the Daytona proposal's "Phase 1: Interface Abstraction" and the comparison table need updating to reflect that Kata is gone and gVisor + Docker are the current defaults.

Actually, this file is a proposal ("Status: Proposal — not implemented"), so it's fair to keep it as-is since it describes what would have been removed at the time of writing. But it would be misleading to say Kata is "the default" if it's been removed. Let me update the comparison.

### 4. `docs/MANUAL.md` — Seems generally okay

Line 5 still says "starts an OpenCode agent" - this should mention Claude Code too, since that's now an option.

Actually, let me look more carefully... line 5 says "starts an OpenCode agent, executes a prompt, and reports progress." With the Claude Code harness, it could also be a Claude Code agent. This is a minor thing.

### 5. `docs/SCHEDULES.md` — Looks okay

### 6. `docs/REVIEWS.md` — Looks okay

### 7. `docs/REVIEWER.md` — This is an implementation plan doc

It references file-pattern auto-review which was removed. This is noted as an implementation plan doc — it may be outdated. But it's a separate document. Given the scope of changes, I should update frontmatter or mark relevant sections.

### 8. `CHANGELOG.md` — Already updated

### 9. `AGENTS.md` — Already updated

Let me now check the current state of some key source files to verify my understanding.

</details>

*Tokens: 61290 total, 1700 in, 303 out*

## Assistant

<details><summary>Reasoning</summary>

Good, now I have a clear picture. Let me also check what execution modes are available in the runner code.

</details>

*Tokens: 62813 total, 1422 in, 184 out*

## Assistant

<details><summary>Reasoning</summary>

Now I know the exact state:
- Execution modes: `local` and `docker` only (Kata removed)
- gVisor: when `USE_GVISOR=true`, Docker containers run with `--runtime=runsc`
- The runner does NOT use containerd anymore

Let me also check the `executionMode()` function.

</details>

*Tokens: 63747 total, 786 in, 171 out*

## Assistant

<details><summary>Reasoning</summary>

Let me check what the executionMode function returns.

</details>

*Tokens: 63939 total, 69 in, 116 out*

## Assistant

<details><summary>Reasoning</summary>

Now I have a complete picture. The default execution mode is now "docker" (with optional gVisor via `--runtime=runsc`). Kata is fully removed.

Let me now plan all the documentation updates:

### Files to update:

#### 1. `README.md`

- **Line 5**: Says "start an OpenCode agent" — should mention Claude Code too
- **Lines 613-739 — Sandbox Isolation section**:
  - Line 625: "The runner integrates gVisor through containerd" → Wrong; now uses Docker `--runtime=runsc`
  - Lines 629-635: Comparison table still lists Kata — should remove Kata row or note it's removed
  - Lines 645-731: "Enabling gVisor on a standard Kubernetes cluster" describes containerd DaemonSet approach — this is now outdated since the runner uses Docker (not containerd) for gVisor. However, the README already has two separate sections: one for k3s local testing with gVisor (lines 282-303) which looks correct, and this one for general K8s. 
  
  Wait, let me re-read the README sandbox section more carefully. It says:
  
  Line 625: "The runner integrates gVisor through containerd — the same `ctr` interface already used for other runtimes, just with `--runtime io.containerd.runsc.v1` instead of `--runtime io.containerd.runc.v2`."
  
  This is now incorrect because:
  1. The runner no longer uses containerd at all
  2. gVisor is integrated via Docker's `--runtime=runsc`
  3. The "other runtimes" reference to containerd is from the Kata era

  The containerd DaemonSet approach in lines 647-731 is from the Kubernetes perspective (installing gVisor on cluster nodes as a RuntimeClass), which is correct for K8s but the justification and integration description is wrong.

  Actually, wait. Let me re-read more carefully. The section talks about gVisor on Kubernetes. On Kubernetes, gVisor IS still configured through containerd's CRI (the RuntimeClass handler configures containerd). So the DaemonSet that installs `runsc` and updates containerd config is still the right approach for Kubernetes.

  But the runner itself doesn't use containerd anymore - the runner uses Docker. The question is: on Kubernetes, does the runner use Docker? No, on K8s it uses containerd via the container runtime. But the runner's execution mode is "docker" via shelling out to `docker run`...

  Actually, I need to check: the runner is deployed as a K8s pod. Inside that pod, it calls `docker run`. If the pod has the Docker socket mounted, it uses the host's Docker daemon. On Kubernetes, you typically don't mount Docker socket into pods - you use the K8s API directly. So the `deploy/k8s/runner-deployment.yaml` might mount the Docker socket...

  Let me check the K8s deployment.

  Actually, looking at the `deploy/compose.yaml` change (e2934c4), it mounts the Docker socket for compose deployments. For K8s, the runner-deployment.yaml likely does the same.

  The gVisor section in the README currently describes two scenarios:
  1. **gVisor on Kubernetes** (lines 645-731): Using DaemonSet + RuntimeClass to make gVisor available as a K8s runtime. Then runner pods and agent pods can use `runtimeClassName: gvisor`.
  2. **gVisor on Docker** (lines 513-553): Using `--runtime=runsc` via `USE_GVISOR=true`.

  The containerd reference on line 625 is about how gVisor integrates at the infrastructure level, not at the runner level. But the phrase "the same `ctr` interface already used for other runtimes" is now wrong because Kata/containerd was removed from the runner.

  I think the most accurate fix is:
  - Update line 625 to say the runner integrates gVisor through Docker's `--runtime runsc` flag
  - Remove the "ctr" reference
  - Update the comparison table to remove Kata or mark it as removed
  - Maybe note that the DaemonSet approach for K8s RuntimeClass still works for cluster-wide gVisor

  Also, line 638: "Kata Containers were removed from Chetter. They cannot expose a port..." — this is correct, it documents the removal.

- The "Execution Environments" part is not in README.md — it's in `docs/FEATURES.md`.

#### 2. `docs/FEATURES.md` — Major updates needed

- **Line 3**: Title says "Complete list of everything Chetter can do today" — needs to match current state
- **Lines 136-154 — Execution Environments section**: 
  - "Kata Containers (default)" is completely wrong. Kata is removed. Should be "Docker (default)" with gVisor as optional sandbox.
  - The overall section should describe Docker + gVisor + Local modes
  - Remove Kata details (Linux bridges, veth pairs, iptables, DNS proxy details are outdated)
- **Lines 382-397 — Runner Configuration table**: References old `runner.yaml` fields. After the Kata removal, the `runtime` field is still there but only `docker` and `local` are valid values.
- **Line 113**: Agent selection mentions "choose `opencode` (default) or `claude-code` harness" — this seems still correct
- **Line 288**: "MCP Tool Reference (26 tools)" - need to count actual tools to verify

Let me count the tools as defined in the source:
From the MCP tools table in FEATURES.md:
- Task Management: 7 tools (submit_task, task_status, list_tasks, cancel_task, task_export, clear_queue, task_events)
- Task Monitoring: 2 (task_progress, task_latest_event)
- Triggers: 5 (create, update, list, delete, run)
- Fleet Health: 1 (runner_health)
- Token Management: 3 (create, list, delete)
- Team/User Management: 4 (create_team, list_teams, delete_team, list_users)
- Schedule Runs: 1 (list_schedule_runs)
- Arcane Scanning: 5 (scanner_status, environment_summary, list_images, image_summary, list_vulnerabilities)

Total: 7+2+5+1+3+4+1+5 = 28 tools

So "26 tools" is wrong. Let me count from the actual source code to be sure.

Actually, let me just verify by checking the tools.go file.

#### 3. `docs/DAYTONA.md`

- Line 9: "replacing the runner's Kata Containers + containerd infrastructure" — no longer relevant since Kata is gone
- Lines 60-65: The comparison table lists "Kata (default)" — should be "Docker (default)"
- Lines 35-36: "Current Kata runners run on your own infra" — outdated
- Lines 62-63: "Kata (default)" under Proposed Architecture table — should change

Actually, since this is a proposal doc ("not implemented"), I should minimize changes but keep facts accurate. The proposal was written assuming Kata was the backend; now Kata is gone so the background context changed.

#### 4. `docs/MANUAL.md`

- Line 5: "starts an OpenCode agent" — should mention Claude Code support
- Line 344: References outdated sections

Let me now build the todo list and implement changes.

Let me first count tools from the source to verify.

</details>

*Tokens: 66257 total, 335 in, 367 out*

## Assistant

<details><summary>Reasoning</summary>

The K8s deployment mounts the Docker socket. So on K8s, the runner uses Docker mode (not containerd), just like in compose. This confirms:

1. The runner uses `docker run` even on K8s (via Docker socket mount)
2. gVisor container-level integration is via `--runtime=runsc` in `docker run`
3. containerd is NOT used by the runner at all — all container/network management is Docker
4. The K8s RuntimeClass approach (gvisor-runtimeclass.yaml) would apply at the pod level (to the runner pod itself), not at the agent container level

Now I also need to check the tool count differently. Let me look at the tool definitions.

</details>

*Tokens: 67150 total, 2672 in, 197 out*

## Assistant

<details><summary>Reasoning</summary>

28 tools total. The FEATURES.md says "26 tools" — that's wrong, should be 28.

Now let me also check the internal/service/runner_rpc.go exports to see if there are more tools there.

Actually wait, I also see 28 grep matches `"chetter_\\w+"` but there could be additional tools not matching that pattern. Let me check if there are other patterns.

Actually 28 matches from `grep -oP '"chetter_\\w+"'` seems comprehensive. Let me count them manually:
1. chetter_arcane_environment_summary
2. chetter_arcane_image_summary
3. chetter_arcane_list_images
4. chetter_arcane_list_vulnerabilities
5. chetter_arcane_scanner_status
6. chetter_cancel_task
7. chetter_clear_queue
8. chetter_create_team
9. chetter_create_token
10. chetter_create_trigger
11. chetter_delete_team
12. chetter_delete_token
13. chetter_delete_trigger
14. chetter_list_schedule_runs
15. chetter_list_tasks
16. chetter_list_teams
17. chetter_list_tokens
18. chetter_list_triggers
19. chetter_list_users
20. chetter_run_trigger
21. chetter_runner_health
22. chetter_submit_task
23. chetter_task_events
24. chetter_task_export
25. chetter_task_latest_event
26. chetter_task_progress
27. chetter_task_status
28. chetter_update_trigger

Yes, 28 tools. FEATURES.md says 26 — needs to be updated to 28.

Now let me plan and implement all changes.

### Summary of changes needed:

#### 1. `README.md` changes:
a) Line 5: Add Claude Code mention ("start an OpenCode agent" → "start an OpenCode or Claude Code agent")
b) Lines 625-626: Fix gVisor integration description - remove containerd reference, describe Docker `--runtime runsc` approach
c) Lines 629-635: Remove Kata from comparison table (it's removed), or update to show current state

#### 2. `docs/FEATURES.md` changes:
a) Lines 136-154: Replace "Kata Containers (default)" section with "Docker (default)" execution environment description, remove Kata details (veth, iptables, DNS proxy)
b) Line 288: Fix tool count from 26 to 28
c) Lines 382-397: Update Runner Configuration - `execution.harness` is correct, but `runtime` field is only `docker`/`local` now

#### 3. `docs/DAYTONA.md` changes:
a) Line 9: Update "Kata Containers + containerd" reference to current state
b) Line 62-63: Fix default backend from "Kata" to "Docker"

#### 4. `docs/MANUAL.md` changes:
a) Line 5: Add Claude Code mention

Let me now implement these changes.

Wait, I need to re-think the README sandbox changes more carefully. Let me re-read:

Lines 621-626:
```
### gVisor is the chosen path

Chetter uses [gVisor](https://gvisor.dev/) (`runsc`) as its sandboxed execution runtime. gVisor provides an application kernel (the Sentry) written in Go that intercepts every system call the container makes and implements the Linux ABI in userspace. The application never touches the host kernel directly. This gives kernel-level isolation without VMs, without custom AMIs, and without sacrificing streaming interaction.

The runner integrates gVisor through containerd — the same `ctr` interface already used for other runtimes, just with `--runtime io.containerd.runsc.v1` instead of `--runtime io.containerd.runc.v2`. Normal container networking works, so the interactive `opencode serve` flow (create session, send prompt, watch events, export session) functions exactly as it does with plain Docker.
```

The second paragraph is wrong because:
1. The runner no longer uses containerd/ctr
2. gVisor is integrated through Docker's `--runtime=runsc` flag
3. There are no "other runtimes" through containerd anymore

I should rewrite this paragraph to say something like:
"The runner integrates gVisor via Docker's `--runtime=runsc` flag, enabled by setting `USE_GVISOR=true`. Normal container networking works..."

Lines 629-635 - the comparison table still has Kata row. Since Kata has been removed, I need to either remove the row or add a note. Actually, the table compares gVisor with alternatives. Since Kata was removed, I should remove the Kata row and add a note that Kata was removed.

Actually, looking again, lines 636-643 go on to explain why Kata was removed. So lines 637-643 say:
"Kata Containers were removed from Chetter. They cannot expose a port from the micro-VM back to the runner host through ctr, so the interactive serve flow is impossible — Kata runs the agent as a batch command and scrapes stdout. They also require nested virtualization and custom AMIs on managed Kubernetes, defeating operational simplicity."

This is actually correct as documentation of the removal. The problem is the comparison table above (lines 629-635) still lists Kata as an option. I should remove the Kata row from the table and keep the explanation paragraph.

OK, now let me also think about the "Enabling gVisor on a standard Kubernetes cluster" section (lines 645-731). This describes a DaemonSet approach that installs gVisor via containerd on K8s nodes. While this is a valid K8s approach, it's somewhat unrelated to how Chetter's runner uses gVisor (which is via Docker). But it's still useful information for someone deploying on K8s who wants gVisor available as a RuntimeClass.

I should add a note that the K8s RuntimeClass approach applies to the runner pod itself, while agent containers use gVisor through Docker's `--runtime=runsc` when `USE_GVISOR=true`.

Actually, I think the current text is fine for the K8s section because it describes how to make gVisor available on K8s nodes, which is a prerequisite for using gVisor on K8s at all. The key issue is just the runner-level integration description in the paragraph above.

Let me also note that the `SECRETS FORWARDED` section in FEATURES.md mentions `ANTHROPIC_API_KEY` which is correct for Claude Code.

OK, let me proceed with the edits now.

Also, I need to double-check whether `deploy/k8s/gvisor-runtimeclass.yaml` references containerd. Let me check.

</details>

*Tokens: 68965 total, 323 in, 87 out*

## Assistant

<details><summary>Reasoning</summary>

Good - the K8s RuntimeClass uses the apt-based gVisor install approach (not the containerd config approach detailed in the README). The README's DaemonSet approach in the Sandbox section (lines 645-731) uses a different method (the Google gVisor container image with containerd config patching). Both are valid but the deploy/k8s version is simpler.

I should update the README to be consistent with the shipped deploy/k8s/gvisor-runtimeclass.yaml instead of the more complex containerd approach.

Now let me implement all the changes. Let me start with FEATURES.md (the biggest changes), then README.md, then the smaller docs.

Let me do the edits:

### FEATURES.md edits:

**1. Execution Environments section (lines 136-154)**
Replace "Kata Containers (default)" heading from line 137 to "Docker (default)" and rewrite the content. Remove veth pairs/iptables/DNS proxy content that belonged to Kata. Replace with Docker + gVisor description.

**2. Tool count (line 288)**
Change "26 tools" to "28 tools"

**3. Runner Configuration table (lines 382-397)** 
The `runtime` field values need updating - only `docker` (default) and `local` are valid now. The `harness` field values are `opencode` (default), `claude-code`.

Actually, let me check if `codex` is still mentioned anywhere.

**4. Execution Environments section header** 
Line 136 says "### Kata Containers (default)" -> change to "### Docker (default)"

### README.md edits:

**1. Line 5**: "start an OpenCode agent" -> "start an OpenCode or Claude Code agent"

**2. Lines 625-626**: Rewrite gVisor integration paragraph

**3. Lines 629-635**: Remove Kata row from comparison table

### DAYTONA.md edits:

**1. Line 9**: Update "Kata Containers + containerd" reference

**2. Lines 62-63**: Update default backend

### MANUAL.md edits:

**1. Line 5**: Add Claude Code mention

Let me now start making the edits.

</details>

Now I have a full picture. Let me implement the documentation updates.

*Tokens: 70238 total, 649 in, 250 out*

## Assistant

<details><summary>Reasoning</summary>

Let me start with FEATURES.md. I need to:

1. Replace executor environments section (lines 136-154): Change "Kata Containers (default)" to "Docker (default)" and rewrite content
2. Fix tool count from 26 to 28 (line 288)
3. Update runner configuration table (lines 382-397) to reflect only valid modes

Let me make these edits.

</details>

*Tokens: 70957 total, 950 in, 416 out*

## Assistant

<details><summary>Reasoning</summary>

Now fix the tool count. Let me re-read the file to find the exact line.

</details>

*Tokens: 71276 total, 60 in, 286 out*

## Assistant

<details><summary>Reasoning</summary>

Now let me update the Runner Configuration table in FEATURES.md. The Runtime field still exists in code but only `docker` (default) and `local` are relevant now.

</details>

*Tokens: 71503 total, 134 in, 164 out*

## Assistant

<details><summary>Reasoning</summary>

Let me read the area around line 382 more carefully.

</details>

*Tokens: 71665 total, 115 in, 114 out*

## Assistant

<details><summary>Reasoning</summary>

Let me try reading the file around the relevant section.

</details>

*Tokens: 71916 total, 239 in, 114 out*

## Assistant

<details><summary>Reasoning</summary>

I see the table doesn't have the `runtime` field anymore — it was removed when the file was last edited (probably in the initial creation of FEATURES.md which was in the 7c5b31a commit). Actually, looking at the original content I read, the table does NOT include a `runtime` row explicitly. It just has `execution.harness`. 

Wait, I was confused looking at the initial output. Let me re-read my original read of FEATURES.md lines 382-397:

```
382: | `runner.workspace_root` | — | `/var/lib/runner` | Workspace directory |
383: | `runner.max_concurrent` | — | `10` | Max concurrent tasks |
384: | `proxy.listen_addr` | — | `:18080` | HTTP proxy address |
385: | `proxy.allowed_domains` | — | (empty) | Egress allowlist |
386: | `proxy.blocked_domains` | — | (empty) | Egress blocklist |
387: | `dns.listen_addr` | — | `:53` | DNS proxy address |
388: | `dns.upstream` | — | `8.8.8.8:53` | Upstream DNS |
389: | `execution.harness` | — | (empty) | `claude-code`, `codex`, or default=opencode |
390: | `deploy.provider` | — | `local` | `local` or `preview` |
391: | `deploy.registry` | — | (empty) | Container registry |
392: | `chetter_mcp.url` | — | (empty) | MCP server URL injected into agents |
```

So the `runtime` field is NOT in this table in FEATURES.md. Good - I already fixed the `execution.harness` row. The file seems complete now. Let me also check if there's a `runtime` field that should be added to the table or if it's intentionally omitted. Looking at the current config code, `Runtime` is part of `ExecutionConfig` but it's not documented in the table. Since `docker` is the default and only `docker`/`local` are valid, and there's no env var for it (it's set by explicit config), it doesn't need to be in the env var table.

Actually, wait - I just successfully edited `execution.harness` line. Let me verify the edit was applied.

</details>

*Tokens: 73078 total, 622 in, 114 out*

## Assistant

<details><summary>Reasoning</summary>

Good, the `execution.harness` line is fixed. Now let me also verify the execution environments and tool count.

</details>

*Tokens: 73555 total, 58339 in, 214 out*

## Assistant

<details><summary>Reasoning</summary>

FEATURES.md looks good. Now let me update README.md.

</details>

*Tokens: 74244 total, 550 in, 207 out*

## Assistant

<details><summary>Reasoning</summary>

Now update README.md. Changes needed:

1. Line 5: "start an OpenCode agent" → "start an OpenCode or Claude Code agent"
2. Lines 625-626: Fix gVisor integration description (remove containerd/ctr reference, describe Docker approach)
3. Lines 629-635: Remove Kata row from comparison table
4. Line 637-643: Keep the Kata removal paragraph but it's fine as-is

Let me also check the comparison table to see the exact lines.

</details>

*Tokens: 74111 total, 73892 in, 111 out*

## Assistant

<details><summary>Reasoning</summary>

Now I can see the exact content. Let me make the changes:

1. Line 5: Fix "start an OpenCode agent" to "start an OpenCode or Claude Code agent"
2. Lines 625: Fix gVisor integration description
3. Lines 629-635: Remove Kata from the comparison table and move the "Kata Containers were removed" note after it
4. Lines 645-731: The "Enabling gVisor on a standard Kubernetes cluster" section uses a different DaemonSet approach than `deploy/k8s/gvisor-runtimeclass.yaml`. I should align it with the shipped manifest or at least note the shipped alternative.

Actually, the README DaemonSet is an alternative approach (using the Google gVisor container image). The deploy/k8s version uses apt-get. Both work. But the reference to containerd-shim is outdated for the runner's use case since gVisor is used via Docker's `--runtime=runsc`.

Let me update the gVisor on K8s section to match the shipped `deploy/k8s/gvisor-runtimeclass.yaml` approach, and add a note about how the runner uses it.

Actually, on second thought, the README is describing how to make gVisor available on K8s nodes as a RuntimeClass, which is a valid approach irrespective of how the runner uses it. The `deploy/k8s/gvisor-runtimeclass.yaml` and the README's DaemonSet are two different ways to install gVisor on K8s nodes. The deploy/k8s version is simpler (uses apt-get directly).

The README approach using `gcr.io/gvisor-containers/runsc:latest` is the more traditional Google-provided method. Both work. Let me update the README to align with the simpler shipped approach.

Actually, I think the most important thing is to:
1. Update the incorrect gVisor integration description
2. Remove Kata from the comparison table
3. Minor updates to K8s section to note the shipped yaml

Let me do the edits now.

</details>

*Tokens: 77370 total, 1272 in, 1674 out*

## Assistant

<details><summary>Reasoning</summary>

Now let me also update the gVisor section that follows the RuntimeClass step, since it may also need updates. Let me check the remaining content after my edits.

</details>

*Tokens: 77563 total, 106 in, 111 out*

## Assistant

<details><summary>Reasoning</summary>

Good. Now let me also update the next section that follows - the "Enabling gVisor on a standard Kubernetes cluster" section. Let me check what's around there now.

</details>

*Tokens: 78475 total, 887 in, 111 out*

## Assistant

<details><summary>Reasoning</summary>

The gVisor on K8s section looks good now. However, I should also add a note after the RuntimeClass section explaining the relationship between the K8s RuntimeClass and the runner's Docker-based gVisor approach. Since the runner now uses Docker directly (not containerd/CRI), the `runtimeClassName: gvisor` on the runner pod only affects the runner container itself, not the agent containers it spawns. Agent containers use gVisor through `USE_GVISOR=true` → `docker run --runtime=runsc`.

Let me also update paragraph 3 to clarify this. Line 661-676 explains `runtimeClassName: gvisor` but doesn't explain the agent container gVisor distinction.

</details>

*Tokens: 79732 total, 708 in, 411 out*

## Assistant

<details><summary>Reasoning</summary>

Now let me also update the "Deploying with Docker + gVisor" section to match. Let me check if it needs changes.

</details>

*Tokens: 79887 total, 643 in, 111 out*

## Assistant

<details><summary>Reasoning</summary>

The gVisor on Kubernetes section at lines 505-509 still mentions "DaemonSet" which now points to the updated Sandbox section. Lines 509-510 are correct and clear about the two levels of gVisor. This is good.

Now let me check the "Deploying with Docker + gVisor" section to make sure it's still accurate.

</details>

*Tokens: 80634 total, 574 in, 111 out*

## Assistant

<details><summary>Reasoning</summary>

This Docker + gVisor section is correct. Good.

Now let me update DAYTONA.md and MANUAL.md.

</details>

