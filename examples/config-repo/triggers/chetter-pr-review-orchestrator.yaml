# Example trigger: multi-agent PR review orchestration for the Chetter repo.
#
# Set enabled: true in your own config repo after confirming that:
# - the GitHub App is installed on the target repository
# - CHETTER_MCP_AUTH_TOKEN is available to trusted runners
# - the trusted self-hosted security limitation is acceptable
# - the model/provider choices below exist in your model catalog

name: chetter-pr-review-orchestrator
enabled: false
trigger_type: pr_review
repo: flatout-works/chetter
agent: review-orchestrator
skills:
  - pr-review-workflow
mcp_profiles:
  - chetter-orchestration
provider_id: synthetic
model_id: hf:zai-org/GLM-5.2
harness: opencode
timeout_sec: 7200
prompt: |-
  Coordinate a complete multi-agent review for this pull request.

  Start a standard review task and an adversarial review task. Wait for both
  to finish using chetter_task_state only, then start an unprivileged
  review-synthesizer task with definition_repo set to the base repo and
  task_export_files so the server copies child exports into its workspace
  without returning transcript text to you. After the synthesizer finishes,
  verify the current PR head and post exactly one Chetter PR review using
  body_task_export_id to extract the synthesizer's marked final review body.

  Do not review the code yourself. Do not attach Chetter MCP profiles or
  GitHub write inheritance to the synthesizer. Do not read child or
  synthesizer transcripts, summaries, errors, progress, or events in the
  orchestrator context.
