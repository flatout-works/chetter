apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chetter-runner-workspaces
  namespace: chetter
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
# Set storageClassName to an RWX-capable class available in your cluster.
