apiVersion: v1
kind: Service
metadata:
  name: chetter-mcp
  namespace: chetter
spec:
  selector:
    app: chetter-mcp
  ports:
  - name: mcp
    port: 8080
    targetPort: 8080
  - name: web
    port: 8090
    targetPort: 8090
